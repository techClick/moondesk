import styled from 'styled-components';

export const Container = styled.div`
  background: white;
  padding: 15px 60px;
  position: absolute;
  z-index: 5;
  margin: auto;
  bottom: 50%;
  right: 50%;
  transform: translate(50%, 35%);
  border-radius: 4px;
`;
