import styled from 'styled-components';

export const Container = styled.div`
  background: white;
  width: 90%;
  max-width: 300px;
  padding: 20px;
  position: fixed;
  z-index: 5;
  margin: auto;
  bottom: 50%;
  right: 50%;
  transform: translate(50%, 35%);
`;
