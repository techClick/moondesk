import React from 'react';
import * as S from './AdditionDialogue.styled';

const AdditionDialogue = function AdditionDialogue(
  { onComplete, label }:
  { onComplete: Function, label: string },
) {
  return (
    <S.Container>
      ready
    </S.Container>
  );
};

export default AdditionDialogue;
